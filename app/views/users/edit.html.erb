<%= form_for(@user) do |f| %>
  <% if @user.icon? %>
    <%= image_tag @user.icon.url %>
  <% else %>
    <%= image_tag("/public/images/no-image.png") %>
  <% end %>
  <%= f.file_field :icon %>
  <%= f.submit 'プロフィール編集を完了する', class: 'btn btn-primary' %>
<% end %>
